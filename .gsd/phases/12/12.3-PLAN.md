---
phase: 12
plan: 3
wave: 3
---

# Plan 12.3: Pro Heatmap Intelligence

## Objective
Implement Section 10 from the Audit (Locked Map Preview) and the Client-side Heatmap Filters.

## Context
- .gsd/redesign/dossier/AuroraLens_Dossier_Page_Audit.md
- frontend/src/components/LocationMap.tsx
- frontend/src/store/useAppStore.ts

## Tasks

<task type="auto">
  <name>Map Locked Preview Card (Section 10)</name>
  <files>
    - frontend/src/components/ui/DestinationDossier.tsx
    - frontend/src/components/ui/dossier/MapProPreview.tsx [NEW]
  </files>
  <action>
    - Replace the single "PRO" button with a blurred image card (Audit Issue 05).
    - Add "Upgrade to Pro" primary CTA inside the card.
    - Overlay lock icon and specific feature text (Audit Issue 05).
  </action>
  <verify>Card correctly renders with blurred background and lock icon.</verify>
  <done>Interactive Sector Map CTA transformed into a conversion-oriented Locked Preview Card.</done>
</task>

<task type="auto">
  <name>Heatmap Client-Side Filtering HUD</name>
  <files>
    - frontend/src/components/LocationMap.tsx
    - frontend/src/components/ui/map/MapFilterHUD.tsx [NEW]
    - frontend/src/store/useAppStore.ts
  </files>
  <action>
    - Add `mapFilters` state and `setMapFilter` to `useAppStore.ts`.
    - Create `MapFilterHUD.tsx` with Intensity Slider and Layer Toggles.
    - Connect `LocationMap.tsx` layers to the store's `mapFilters`.
  </action>
  <verify>Sliding the intensity threshold on the HUD affects the map's aurora opacity or visibility.</verify>
  <done>Functional Map Filter HUD integrated into the dashboard map view.</done>
</task>

## Success Criteria
- [ ] Sector map CTA is now a "Locked Preview Card" and not just a button.
- [ ] Map Filter HUD is accessible in the HUD/Map view.
- [ ] Heatmap filters correctly modulate map layer visibility.
