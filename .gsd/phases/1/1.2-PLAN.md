---
phase: 1
plan: 2
wave: 2
depends_on: ["1.1"]
files_modified: ["src/predictor.py", "api/main.py"]
autonomous: true
---

# Plan 1.2: ML Model Activation

## Objective
Link the pre-trained XGBoost models in `models/` to the `predictor.py` module and expose the probability scoring through the FastAPI endpoints.

## Context
- .gsd/SPEC.md
- src/predictor.py
- src/space_weather.py
- models/

## Tasks

<task type="auto">
  <name>Link ML Models</name>
  <files>src/predictor.py</files>
  <action>
    Update predictor.py to load the best-performing .pkl model from the models/ directory.
    Ensure the preprocessing logic matches the training data format (using space_weather.py attributes).
    AVOID: Using obsolete training scripts for inference.
  </action>
  <verify>pytest tests/test_predictor.py or direct execution check</verify>
  <done>Predictor returns a probability score (0-100) based on input telemetry</done>
</task>

<task type="auto">
  <name>Endpoint Activation</name>
  <files>api/main.py, api/routers/weather.py</files>
  <action>
    Connect the FastAPI forecast route to the predictor's inference results.
  </action>
  <verify>curl http://localhost:8000/api/weather/forecast/global</verify>
  <done>Endpoint returns live ML-driven aurora scores</done>
</task>

## Success Criteria
- [ ] ML inference is functional in the backend.
- [ ] API serves live prediction data.
