# Plan 2.2 Summary: Google Maps 3D & Globe-to-Map

## Work Performed
- **Installed** `@vis.gl/react-google-maps` to power the 3D map view.
- **Created `LocationMap.tsx`**: 
  - A highly polished, full-screen overlay component integrating the Google Maps `<APIProvider>` and `<Map>` components.
  - Configured with `disableDefaultUI` for a cinematic feel and pre-tilted to 65 degrees for 3D buildings.
  - Included a central UI overlay targeting the coordinates, and a sleek "Back to Global View" floating button.
  - Added a smart fallback UI for when the Google Maps API Key is missing.
- **Modified `AuroraGlobe.tsx`**:
  - Added an `onZoomComplete` callback that fires exactly when the GSAP camera zoom animation to Kashmir finishes.
- **Modified `page.tsx`**:
  - Wired the entire orchestrating flow: User clicks "KASHMIR FOCUS" -> Globe plays 2.4s zoom animation -> Animation finishes -> `showMap` state toggles true -> `LocationMap` component fades in over the globe.

## Required Setup
To view the Google Maps 3D buildings, the user must add a valid Google Maps API Key to `.env`:
`NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="AIzaSy..."`

## Next Steps
Proceed to Phase 2.3: Implement local visibility heatmaps and markers inside the Google Map view.
